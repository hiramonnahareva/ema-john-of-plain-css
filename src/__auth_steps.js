/**
 * 1. create a new firebase project in cond=sole.firebase.google.com 
 * 2.npm install firebase 
 * 3. create firebase.init.js and import getAuth to export auth
 * 4. firebase settings > signup component, setup route
 * 5. create Login, setup component, setup route 
 * 6. Attach form field handler and form submit handler 
 * 7. npm install --save react-firebase-hook
 * 8. useCreateUserWithEmailAndPassword from react firebase hooks
 * 9. if user is created redirect to the expected page
 * 10. signInWithEmailAndPassword for login
 * 11. create RequirAuth component ==> check user exists also tract user localion
 * 12. in route wrap protected component by require Auth component
 **/
/**
 * firebase hosting steps
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
 */